# Agent 系统使用指南

## 📚 目录

1. [快速入门](#快速入门)
2. [核心概念](#核心概念)
3. [基础操作](#基础操作)
4. [高级功能](#高级功能)
5. [实战场景](#实战场景)
6. [最佳实践](#最佳实践)
7. [常见问题](#常见问题)

---

## 快速入门

### 什么是 Agent？

Agent 是专门为特定任务类型优化的 AI 配置。就像在团队中，你会找前端工程师做 UI，找后端工程师做 API 一样，Agent 系统让 AI 能够根据任务特点扮演不同的专家角色。

### 5 分钟上手

#### 第 1 步：查看可用的 Agent

```bash
# 在 Claude Desktop 中直接询问
"显示所有可用的 agents"
```

#### 第 2 步：为任务分配 Agent

```bash
# 方式 1：通过任务 ID 分配
"将 frontend developer agent 分配给任务 task-001"

# 方式 2：批量分配
"将所有 UI 相关任务分配给 frontend developer"
```

#### 第 3 步：执行任务

```bash
# Agent 会自动应用其专业知识
"执行任务 task-001"
```

---

## 核心概念

### Agent 的组成部分

每个 Agent 包含以下要素：

| 要素 | 说明 | 示例 |
|------|------|------|
| **名称 (name)** | Agent 的显示名称 | "React 专家" |
| **角色 (role)** | Agent 的职责定位 | "前端工程师" |
| **描述 (description)** | 详细说明 | "专注于 React 最佳实践和性能优化" |
| **指令 (instructions)** | 具体工作指导 | "使用函数式组件和 Hooks" |
| **颜色 (color)** | 视觉标识 | "#61DAFB" (React 蓝) |
| **头像 (avatar)** | 图标标识 | "⚛️" |
| **首选模型 (preferredModel)** | 推荐的 AI 模型 | "claude-3-opus" |

### 内置 Agent 类型

系统预置了 7 种常用 Agent：

#### 1. Frontend Developer (前端开发者) 🎨
- **擅长领域**：UI/UX 实现、组件开发
- **适用任务**：
  - React/Vue/Angular 组件开发
  - CSS 样式和布局
  - 前端性能优化
  - 响应式设计
- **示例指令**："遵循 React Hooks 最佳实践，注重可访问性"

#### 2. Backend Engineer (后端工程师) ⚙️
- **擅长领域**：服务器端逻辑、API 设计
- **适用任务**：
  - RESTful API 开发
  - 数据库查询优化
  - 业务逻辑实现
  - 微服务架构
- **示例指令**："使用 Node.js/Express，遵循 SOLID 原则"

#### 3. DevOps Specialist (运维专家) 🚀
- **擅长领域**：基础设施、自动化部署
- **适用任务**：
  - CI/CD 流水线配置
  - Docker 容器化
  - Kubernetes 编排
  - 监控和日志系统
- **示例指令**："优先考虑可扩展性和容错性"

#### 4. Data Scientist (数据科学家) 📊
- **擅长领域**：数据分析、机器学习
- **适用任务**：
  - 数据清洗和预处理
  - 统计分析
  - 机器学习模型开发
  - 数据可视化
- **示例指令**："使用 Python/pandas，注重数据质量"

#### 5. Security Expert (安全专家) 🔒
- **擅长领域**：安全审计、漏洞评估
- **适用任务**：
  - 代码安全审查
  - 认证授权实现
  - 漏洞扫描和修复
  - 安全配置
- **示例指令**："遵循 OWASP Top 10，实施零信任原则"

#### 6. QA Engineer (测试工程师) ✅
- **擅长领域**：测试策略、质量保证
- **适用任务**：
  - 单元测试编写
  - 集成测试设计
  - E2E 测试实现
  - Bug 验证
- **示例指令**："使用 Jest/Vitest，追求高代码覆盖率"

#### 7. Technical Writer (技术文档工程师) 📝
- **擅长领域**：文档编写、知识管理
- **适用任务**：
  - API 文档
  - 用户手册
  - README 编写
  - 代码注释规范
- **示例指令**："使用清晰简洁的语言，包含示例代码"

---

## 基础操作

### 操作 1：查看 Agent 列表

```bash
# 命令方式
"列出所有可用的 agents"
"显示 agent 模板"

# 通过 Task Viewer (Web UI)
访问: http://localhost:52424?lang=zh-CN
点击左侧菜单 "Agents" 选项
```

### 操作 2：为任务分配 Agent

#### 方法 A：单个任务分配

```bash
# 语法：assign [agent-name] to task [task-id]
"分配 frontend developer 给任务 abc-123"
"让 backend engineer 处理任务 def-456"
```

#### 方法 B：批量分配

```bash
# 按任务类型批量分配
"将所有 UI 任务分配给 frontend developer"
"将所有 API 任务分配给 backend engineer"

# 按任务名称关键词批量分配
"将包含 'component' 的任务分配给 frontend developer"
```

#### 方法 C：通过 Task Viewer 可视化分配

1. 打开 Task Viewer
2. 在左侧 Agent 列表中选择 Agent
3. 拖拽 Agent 到目标任务上
4. 确认分配

### 操作 3：创建自定义 Agent

#### 简单创建

```bash
"创建一个 agent: 性能优化专家，专注于代码性能和优化"
```

#### 详细创建（JSON 配置）

创建文件 `custom-agents.json`：

```json
{
  "agents": [
    {
      "id": "perf-opt-001",
      "name": "性能优化专家",
      "role": "Performance Engineer",
      "description": "专注于应用性能分析和优化",
      "instructions": "分析性能瓶颈，提供具体的优化方案，包括：\n1. 代码层面优化（算法复杂度、内存使用）\n2. 架构层面优化（缓存策略、异步处理）\n3. 基础设施优化（CDN、负载均衡）\n提供基准测试和优化前后对比",
      "color": "#FF6B6B",
      "avatar": "⚡",
      "preferredModel": "claude-3-opus"
    }
  ]
}
```

导入配置：
```bash
"导入 agent 配置文件 custom-agents.json"
```

### 操作 4：查看任务的 Agent 信息

```bash
# 查看特定任务的 agent
"显示任务 abc-123 的 agent"

# 查看所有任务的 agent 分配情况
"显示所有任务的 agent 分配"
```

### 操作 5：移除 Agent 分配

```bash
# 移除单个任务的 agent
"移除任务 abc-123 的 agent"

# 批量移除
"移除所有任务的 agent 分配"
```

---

## 高级功能

### 功能 1：Agent 协作

多个 Agent 可以协同处理复杂项目：

#### 场景：开发一个用户注册功能

```bash
# 步骤 1：任务规划
"创建用户注册功能的任务拆分"

# 系统生成的任务和建议的 agent 分配：
# 任务 1: 设计 UI 界面 → Frontend Developer
# 任务 2: 实现注册 API → Backend Engineer  
# 任务 3: 添加输入验证 → Security Expert
# 任务 4: 编写单元测试 → QA Engineer
# 任务 5: 编写 API 文档 → Technical Writer

# 步骤 2：批量分配
"按建议分配 agents"

# 步骤 3：按依赖顺序执行
"按依赖关系执行所有任务"
```

### 功能 2：Agent 指令优化

为 Agent 提供更精确的指令：

```json
{
  "id": "react-expert-001",
  "name": "React 高级专家",
  "instructions": "# React 开发指南\n\n## 代码规范\n- 使用 TypeScript 严格模式\n- 函数式组件 + Hooks\n- Props 使用 interface 定义\n\n## 性能优化\n- 使用 React.memo 防止不必要的重渲染\n- 使用 useMemo 和 useCallback 优化计算\n- 懒加载大型组件\n\n## 状态管理\n- 简单状态用 useState\n- 复杂状态用 useReducer\n- 全局状态用 Context + useContext\n\n## 测试要求\n- 每个组件编写单元测试\n- 覆盖率 > 80%\n\n## 文档要求\n- 组件顶部添加 JSDoc 注释\n- Props 必须有详细说明"
}
```

### 功能 3：条件化 Agent 选择

根据任务特征自动选择 Agent：

```bash
# 设置自动分配规则
"创建 agent 分配规则：
- 任务名称包含 'UI' 或 'component' → Frontend Developer
- 任务名称包含 'API' 或 'endpoint' → Backend Engineer
- 任务名称包含 'test' → QA Engineer
- 任务名称包含 'security' → Security Expert"

# 应用规则
"应用 agent 自动分配规则到所有待处理任务"
```

### 功能 4：Agent 模板导出与分享

```bash
# 导出当前 agent 配置
"导出 agent 'React 专家' 的配置到文件"

# 分享给团队
"生成 agent 模板分享链接"

# 导入团队成员的 agent
"从文件导入 agent 模板 team-agents.json"
```

---

## 实战场景

### 场景 1：全栈 Web 应用开发

**项目**：开发一个博客系统

#### 步骤 1：任务拆分

```bash
"为博客系统创建任务拆分，包括前端、后端、测试和文档"
```

生成的任务：
1. 博客列表页面 UI
2. 文章详情页面 UI
3. 文章管理 API
4. 用户认证系统
5. 数据库设计
6. 单元测试
7. E2E 测试
8. API 文档

#### 步骤 2：Agent 分配策略

```bash
# 前端任务
"分配任务 1, 2 给 frontend developer"

# 后端任务
"分配任务 3, 4, 5 给 backend engineer"

# 测试任务
"分配任务 6, 7 给 qa engineer"

# 文档任务
"分配任务 8 给 technical writer"
```

#### 步骤 3：执行顺序

```bash
# 阶段 1：基础设施
"执行任务 5 (数据库设计)"

# 阶段 2：后端开发
"执行任务 3, 4 (API 开发)"

# 阶段 3：前端开发
"执行任务 1, 2 (UI 开发)"

# 阶段 4：测试
"执行任务 6, 7 (测试)"

# 阶段 5：文档
"执行任务 8 (文档)"
```

### 场景 2：代码重构项目

**项目**：重构老旧的 React 项目

#### 自定义 Agent：重构专家

```json
{
  "id": "refactor-expert-001",
  "name": "代码重构专家",
  "role": "Refactoring Specialist",
  "instructions": "重构指导原则：\n1. 保持功能不变\n2. 提高代码可读性\n3. 减少代码重复\n4. 优化性能\n5. 增强可测试性\n\n重构步骤：\n1. 分析现有代码\n2. 识别问题点\n3. 提出改进方案\n4. 逐步重构\n5. 编写测试确保功能不变",
  "color": "#9B59B6",
  "avatar": "🔧"
}
```

#### 任务执行

```bash
# 创建重构任务
"分析项目并创建重构任务列表"

# 分配给重构专家
"将所有重构任务分配给重构专家"

# 协作分配
"将测试任务分配给 qa engineer"
```

### 场景 3：安全审计

**项目**：对现有系统进行安全审查

```bash
# 创建安全审计任务
"创建安全审计任务，包括：
1. 认证系统审查
2. API 权限检查
3. 数据验证审查
4. SQL 注入检测
5. XSS 漏洞扫描"

# 分配给安全专家
"将所有安全审计任务分配给 security expert"

# 执行审计
"按优先级执行所有安全审计任务"
```

---

## 最佳实践

### ✅ DO（推荐做法）

#### 1. 匹配专业领域

```bash
✅ 正确：UI 任务 → Frontend Developer
✅ 正确：API 任务 → Backend Engineer
✅ 正确：测试任务 → QA Engineer
```

#### 2. 提供清晰的 Agent 指令

```json
✅ 好的指令：
{
  "instructions": "使用 TypeScript 严格模式开发 React 组件。\n遵循以下规范：\n- 函数式组件\n- Props 用 interface 定义\n- 使用 Hooks 管理状态\n- 组件必须有单元测试"
}

❌ 模糊的指令：
{
  "instructions": "写 React 代码"
}
```

#### 3. 合理使用 Agent 协作

```bash
✅ 正确：
"任务 1 (UI) → Frontend Developer"
"任务 2 (API) → Backend Engineer"  
"任务 3 (测试) → QA Engineer"

❌ 错误：
"所有任务都分配给 Frontend Developer"
```

#### 4. 定期更新 Agent 配置

```bash
# 每个迭代后回顾和优化
✅ "更新 frontend developer 的指令，添加新的编码规范"
✅ "根据项目经验调整 agent 配置"
```

### ❌ DON'T（不推荐做法）

#### 1. 过度使用 Agent

```bash
❌ 错误：简单任务也分配专门的 agent
"将 '修复拼写错误' 任务分配给 technical writer"

✅ 正确：简单任务直接执行
"直接执行拼写错误修复"
```

#### 2. Agent 指令过于复杂

```bash
❌ 错误：指令长达数千字，包含所有可能的场景
✅ 正确：简洁明确的核心指导原则（200-500 字）
```

#### 3. 忽略任务依赖关系

```bash
❌ 错误：
"同时执行前端和后端任务（但后端 API 未定义）"

✅ 正确：
"先执行后端 API 定义 → 再执行前端集成"
```

#### 4. 使用不合适的模型

```bash
❌ 错误：复杂算法任务使用基础模型
✅ 正确：
{
  "name": "算法专家",
  "preferredModel": "claude-3-opus"  // 使用高级模型
}
```

### 🎯 性能优化建议

#### 1. Agent 指令优化

```json
{
  "instructions": "核心规范：\n1. [最重要的 3-5 条规则]\n2. ...\n\n参考文档：[链接]"
}
```

#### 2. 减少不必要的 Agent 切换

```bash
✅ 好：同类任务批量处理
"执行所有前端任务（frontend developer）"

❌ 差：频繁切换 agent
"执行任务 1 (frontend) → 任务 2 (backend) → 任务 3 (frontend)"
```

#### 3. 使用模板加速配置

```bash
✅ "从模板创建 agent，基于 frontend.json"
❌ "每次手动配置所有 agent 属性"
```

---

## 常见问题

### Q1: Agent 的指令没有被应用？

**可能原因**：
1. Agent ID 或 Task ID 不正确
2. 指令格式错误
3. 指令太长被截断

**解决方案**：
```bash
# 验证分配
"检查任务 abc-123 的 agent 分配状态"

# 重新分配
"移除任务 abc-123 的 agent"
"重新分配 frontend developer 给任务 abc-123"

# 简化指令
"更新 agent 指令为简化版本"
```

### Q2: 如何为不同项目使用不同的 Agent 配置？

**方案 1：使用配置文件**
```bash
# 项目 A
"加载 agent 配置：project-a-agents.json"

# 项目 B  
"加载 agent 配置：project-b-agents.json"
```

**方案 2：使用命名空间**
```json
{
  "id": "project-a-frontend-dev",
  "name": "前端开发者 (项目A)",
  "instructions": "项目 A 特定规范..."
}
```

### Q3: Agent 之间的协作如何确保一致性？

**最佳实践**：

1. **共享文档**
```bash
"创建项目规范文档，所有 agents 遵循"
```

2. **定义接口**
```bash
"先定义 API 接口规范 → 前后端 agents 遵循"
```

3. **使用统一的代码风格**
```json
{
  "globalInstructions": "所有 agents 遵循 Prettier + ESLint 配置"
}
```

### Q4: 如何测试 Agent 的效果？

**测试方法**：

```bash
# 方法 1：A/B 测试
"创建测试任务 X"
"执行任务 X（无 agent）" → 保存结果
"执行任务 X（使用 agent）" → 对比结果

# 方法 2：质量评估
"使用 qa engineer agent 评估其他 agents 的输出质量"

# 方法 3：团队反馈
"收集团队对 agent 输出的反馈，持续优化"
```

### Q5: Agent 可以跨项目复用吗？

**可以！推荐做法**：

```bash
# 导出成功的 agent 配置
"导出 agent 'React 专家' 到 templates/agents/react-expert.json"

# 在新项目中导入
"导入 agent 模板 react-expert.json"

# 根据新项目调整
"更新 agent 指令，适配新项目的技术栈"
```

### Q6: 如何管理大量的自定义 Agent？

**组织策略**：

1. **按职能分类**
```
agents/
├── frontend/
│   ├── react-expert.json
│   ├── vue-expert.json
├── backend/
│   ├── nodejs-expert.json
│   ├── python-expert.json
├── testing/
│   ├── unit-test-expert.json
│   ├── e2e-test-expert.json
```

2. **使用标签**
```json
{
  "id": "react-expert",
  "tags": ["frontend", "react", "typescript", "web"]
}
```

3. **版本控制**
```json
{
  "id": "react-expert-v2",
  "version": "2.0.0",
  "changelog": "添加了 React 18 并发特性支持"
}
```

---

## 附录

### A. Agent 模板示例

#### 前端专家（React）

```json
{
  "id": "frontend-react-expert",
  "name": "React 前端专家",
  "role": "Frontend Developer",
  "description": "专注于 React 生态系统的前端开发",
  "instructions": "# React 开发规范\n\n## 技术栈\n- React 18+\n- TypeScript\n- Hooks\n- React Router\n- React Query\n\n## 代码规范\n1. 使用函数式组件\n2. Props 使用 TypeScript interface\n3. 状态管理优先使用 Hooks\n4. 复杂状态用 useReducer\n5. 组件拆分遵循单一职责\n\n## 性能优化\n- React.memo 防止不必要渲染\n- useMemo/useCallback 优化计算\n- 代码分割和懒加载\n- 虚拟化长列表\n\n## 测试\n- 单元测试覆盖率 > 80%\n- 使用 Testing Library\n- 测试用户交互而非实现细节",
  "color": "#61DAFB",
  "avatar": "⚛️",
  "preferredModel": "claude-3-sonnet"
}
```

#### 后端专家（Node.js）

```json
{
  "id": "backend-nodejs-expert",
  "name": "Node.js 后端专家",
  "role": "Backend Engineer",
  "description": "专注于 Node.js 服务端开发",
  "instructions": "# Node.js 后端开发规范\n\n## 技术栈\n- Node.js (LTS)\n- Express/Fastify\n- TypeScript\n- PostgreSQL/MongoDB\n- Redis\n\n## API 设计\n1. RESTful 规范\n2. 统一的响应格式\n3. 完善的错误处理\n4. API 版本控制\n5. 详细的 OpenAPI 文档\n\n## 安全实践\n- 输入验证（joi/zod）\n- SQL 注入防护\n- XSS 防护\n- CSRF 保护\n- 限流和防 DDoS\n\n## 性能优化\n- 数据库查询优化\n- 合理使用缓存\n- 异步处理耗时任务\n- 连接池管理\n\n## 测试\n- 单元测试（Jest）\n- 集成测试\n- API 测试（Supertest）",
  "color": "#339933",
  "avatar": "🟢",
  "preferredModel": "claude-3-sonnet"
}
```

### B. 快速参考卡片

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 AGENT 系统快速参考
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 查看 Agents
  → "显示所有 agents"
  → "列出可用的 agent 模板"

🎯 分配 Agent
  → "分配 [agent-name] 给任务 [task-id]"
  → "将所有 [类型] 任务分配给 [agent-name]"

➕ 创建 Agent
  → "创建 agent: [名称] 专注于 [领域]"

❌ 移除 Agent
  → "移除任务 [task-id] 的 agent"

🔍 查询状态
  → "显示任务 [task-id] 的 agent"
  → "显示所有任务的 agent 分配"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### C. 相关资源

- 📖 [Agent 系统概述](./agents.md)
- 🛠️ [Task Viewer 使用指南](../../tools/task-viewer/README.md)
- 📚 [API 文档](../api.md)
- 🎓 [最佳实践集锦](./best-practices.md)

---

## 反馈与支持

如有问题或建议，欢迎：
- 提交 Issue：[GitHub Issues](https://github.com/yourusername/mcp-shrimp-task-manager/issues)
- 加入讨论：[GitHub Discussions](https://github.com/yourusername/mcp-shrimp-task-manager/discussions)

---

**文档版本**：v1.0.0  
**最后更新**：2025-01-21  
**维护者**：开发团队




